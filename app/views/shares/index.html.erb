    <style>
      html, body{padding: 0; margin: 0;}
      #earth_div{top: 0; right: 0; bottom: 0; left: 0; position: absolute !important;}
    </style>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
    <script src="http://www.webglearth.com/v2/api.js"></script>
    <script>
      function initialize() {
          $.get( "http://geoip.nekudo.com/api/", function( data ) {
            var latLon = [data.location.latitude, data.location.longitude];
            var city = [data.city]
            console.log( data );
            console.log(latLon);
            console.log(city);
            console.log(gon.share_title);
            var earth = new WE.map('earth_div', {
              center: latLon,
              zoom: 2.125,
              dragging: true,
            });

            var marker = WE.marker(latLon).addTo(earth);
            marker.bindPopup('You are in ' + city + '!!');

            WE.tileLayer('http://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{ 
              attribution: '© OpenStreetMap contributors'
            }).addTo(earth);
          });

      }
    </script>
    <body onload="initialize()">
    <div id="earth_div" class="col-xs-10 col-xs-offset-1 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3"></div> 


<h1>Shares</h1>

<br />
<br />  
<br />  
<br />  
<br />
<br />  
<br />  
<br />  

<div class="col-xs-10 col-xs-offset-1 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
  <% @shares.each do |share| %>
  <div class="booyah-box post">
    <div class="handle"><%= current_user.name %></div>
    <%= link_to share.title, share_path(share) %>
    <p>
      <%= linkify_hashtags(markdown(share.body)) %>
    </p>
    <%= pluralize(share.votes.count, "votes") %>
    
   </div>


  <% end %>
</div>

